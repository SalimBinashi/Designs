import 'package:flutter/cupertino.dart';

class OpenChecks extends StatefulWidget {
  @override
  _OpenChecksState createState() => _OpenChecksState();
}

class _OpenChecksState extends State<OpenChecks> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("No Open Checks")),

    );
  }
}